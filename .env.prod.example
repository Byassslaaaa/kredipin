# Konfigurasi deployment produksi (VPS). Salin menjadi .env di root:
#   cp .env.prod.example .env
# lalu sesuaikan nilainya.

# Domain yang sudah diarahkan (A record) ke IP VPS.
DOMAIN=kredipin.contoh.com

# Email untuk notifikasi sertifikat Let's Encrypt (Caddy).
TLS_EMAIL=anda@contoh.com
